package First;

import javax.swing.JFrame;

import java.awt.Color;

import javax.swing.ImageIcon;
public class Main {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		// JFrame a gui window to add components to 
	/*	JFrame frame = new JFrame(); // create a frame
		frame.setSize(420,420); // sets the x- dimension and y-dimension of frame
		frame.setVisible(true); // make frame visible
		frame.setTitle("AFnaN's window"); // sets title of frame
		
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		// exit on close ---> exit out of application
		// Hide on close ----> running in background
		// DO_NOTHING_ON_CLOSE-->cann't close window
		
		frame.setResizable(false); //prevent frame from begin resized
		ImageIcon image = new ImageIcon("pic.jpg");
		frame.setIconImage(image.getImage());//change icon image frame
		// change color of background
		frame.getContentPane().setBackground(new Color(91,61,69));
	*/
		
		// AnotherProcedure frame = new AnotherProcedure();
		
		
		
		
		
		
	}

}
